apiVersion: v1
kind: Service
metadata:
  name: openstatus-dashboard
  labels:
    app.kubernetes.io/name: openstatus
    app.kubernetes.io/component: dashboard
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: openstatus
    app.kubernetes.io/component: dashboard
  ports:
    - name: http
      port: 3000
      targetPort: 3000
