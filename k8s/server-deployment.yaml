apiVersion: apps/v1
kind: Deployment
metadata:
  name: openstatus-server
  labels:
    app.kubernetes.io/name: openstatus
    app.kubernetes.io/component: server
spec:
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: openstatus
      app.kubernetes.io/component: server
  template:
    metadata:
      labels:
        app.kubernetes.io/name: openstatus
        app.kubernetes.io/component: server
    spec:
      containers:
        - name: server
          image: brunomassaini/openstatus-server:latest
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 3000
              name: http
          envFrom:
            - secretRef:
                name: openstatus-env
          env:
            - name: DATABASE_URL
              value: http://openstatus-libsql:8080
            - name: PORT
              value: "3000"
          readinessProbe:
            httpGet:
              path: /ping
              port: 3000
            initialDelaySeconds: 15
            periodSeconds: 15
          livenessProbe:
            httpGet:
              path: /ping
              port: 3000
            initialDelaySeconds: 30
            periodSeconds: 20
